public struct BenchPackage038 {

    public struct Model1: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_1", score: Double = Double(38 * 1)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model1(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model2: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_2", score: Double = Double(38 * 2)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model2(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model3: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_3", score: Double = Double(38 * 3)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model3(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model4: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_4", score: Double = Double(38 * 4)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model4(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model5: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_5", score: Double = Double(38 * 5)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model5(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model6: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_6", score: Double = Double(38 * 6)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model6(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model7: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_7", score: Double = Double(38 * 7)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model7(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model8: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_8", score: Double = Double(38 * 8)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model8(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model9: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_9", score: Double = Double(38 * 9)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model9(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model10: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_10", score: Double = Double(38 * 10)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model10(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model11: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_11", score: Double = Double(38 * 11)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model11(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model12: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_12", score: Double = Double(38 * 12)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model12(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model13: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_13", score: Double = Double(38 * 13)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model13(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model14: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_14", score: Double = Double(38 * 14)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model14(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model15: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_15", score: Double = Double(38 * 15)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model15(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model16: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_16", score: Double = Double(38 * 16)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model16(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model17: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_17", score: Double = Double(38 * 17)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model17(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model18: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_18", score: Double = Double(38 * 18)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model18(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model19: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_19", score: Double = Double(38 * 19)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model19(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model20: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_20", score: Double = Double(38 * 20)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model20(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model21: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_21", score: Double = Double(38 * 21)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model21(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model22: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_22", score: Double = Double(38 * 22)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model22(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model23: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_23", score: Double = Double(38 * 23)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model23(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model24: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_24", score: Double = Double(38 * 24)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model24(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model25: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_25", score: Double = Double(38 * 25)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model25(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model26: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_26", score: Double = Double(38 * 26)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model26(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model27: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_27", score: Double = Double(38 * 27)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model27(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model28: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_28", score: Double = Double(38 * 28)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model28(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model29: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_29", score: Double = Double(38 * 29)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model29(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model30: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_30", score: Double = Double(38 * 30)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model30(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model31: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_31", score: Double = Double(38 * 31)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model31(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model32: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_32", score: Double = Double(38 * 32)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model32(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model33: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_33", score: Double = Double(38 * 33)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model33(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model34: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_34", score: Double = Double(38 * 34)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model34(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model35: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_35", score: Double = Double(38 * 35)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model35(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model36: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_36", score: Double = Double(38 * 36)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model36(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model37: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_37", score: Double = Double(38 * 37)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model37(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model38: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_38", score: Double = Double(38 * 38)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model38(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model39: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_39", score: Double = Double(38 * 39)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model39(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model40: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_40", score: Double = Double(38 * 40)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model40(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model41: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_41", score: Double = Double(38 * 41)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model41(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model42: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_42", score: Double = Double(38 * 42)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model42(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model43: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_43", score: Double = Double(38 * 43)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model43(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model44: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_44", score: Double = Double(38 * 44)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model44(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model45: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_45", score: Double = Double(38 * 45)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model45(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model46: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_46", score: Double = Double(38 * 46)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model46(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model47: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_47", score: Double = Double(38 * 47)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model47(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model48: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_48", score: Double = Double(38 * 48)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model48(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model49: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_49", score: Double = Double(38 * 49)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model49(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model50: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_50", score: Double = Double(38 * 50)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model50(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model51: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_51", score: Double = Double(38 * 51)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model51(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model52: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_52", score: Double = Double(38 * 52)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model52(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model53: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_53", score: Double = Double(38 * 53)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model53(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model54: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_54", score: Double = Double(38 * 54)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model54(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model55: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_55", score: Double = Double(38 * 55)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model55(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model56: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_56", score: Double = Double(38 * 56)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model56(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model57: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_57", score: Double = Double(38 * 57)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model57(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model58: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_58", score: Double = Double(38 * 58)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model58(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model59: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_59", score: Double = Double(38 * 59)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model59(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model60: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_60", score: Double = Double(38 * 60)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model60(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model61: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_61", score: Double = Double(38 * 61)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model61(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model62: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_62", score: Double = Double(38 * 62)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model62(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model63: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_63", score: Double = Double(38 * 63)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model63(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model64: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_64", score: Double = Double(38 * 64)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model64(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model65: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_65", score: Double = Double(38 * 65)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model65(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model66: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_66", score: Double = Double(38 * 66)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model66(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model67: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_67", score: Double = Double(38 * 67)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model67(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model68: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_68", score: Double = Double(38 * 68)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model68(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model69: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_69", score: Double = Double(38 * 69)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model69(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model70: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_70", score: Double = Double(38 * 70)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model70(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model71: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_71", score: Double = Double(38 * 71)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model71(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model72: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_72", score: Double = Double(38 * 72)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model72(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model73: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_73", score: Double = Double(38 * 73)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model73(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model74: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_74", score: Double = Double(38 * 74)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model74(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model75: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_75", score: Double = Double(38 * 75)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model75(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model76: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_76", score: Double = Double(38 * 76)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model76(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model77: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_77", score: Double = Double(38 * 77)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model77(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model78: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_78", score: Double = Double(38 * 78)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model78(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model79: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_79", score: Double = Double(38 * 79)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model79(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model80: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_80", score: Double = Double(38 * 80)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model80(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model81: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_81", score: Double = Double(38 * 81)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model81(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model82: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_82", score: Double = Double(38 * 82)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model82(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model83: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_83", score: Double = Double(38 * 83)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model83(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model84: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_84", score: Double = Double(38 * 84)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model84(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model85: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_85", score: Double = Double(38 * 85)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model85(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model86: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_86", score: Double = Double(38 * 86)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model86(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model87: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_87", score: Double = Double(38 * 87)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model87(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model88: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_88", score: Double = Double(38 * 88)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model88(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model89: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_89", score: Double = Double(38 * 89)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model89(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model90: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_90", score: Double = Double(38 * 90)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model90(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model91: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_91", score: Double = Double(38 * 91)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model91(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model92: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_92", score: Double = Double(38 * 92)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model92(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model93: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_93", score: Double = Double(38 * 93)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model93(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model94: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_94", score: Double = Double(38 * 94)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model94(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model95: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_95", score: Double = Double(38 * 95)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model95(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model96: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_96", score: Double = Double(38 * 96)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model96(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model97: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_97", score: Double = Double(38 * 97)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model97(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model98: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_98", score: Double = Double(38 * 98)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model98(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model99: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_99", score: Double = Double(38 * 99)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model99(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model100: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_100", score: Double = Double(38 * 100)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model100(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model101: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_101", score: Double = Double(38 * 101)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model101(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model102: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_102", score: Double = Double(38 * 102)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model102(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model103: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_103", score: Double = Double(38 * 103)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model103(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model104: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_104", score: Double = Double(38 * 104)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model104(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model105: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_105", score: Double = Double(38 * 105)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model105(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model106: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_106", score: Double = Double(38 * 106)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model106(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model107: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_107", score: Double = Double(38 * 107)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model107(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model108: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_108", score: Double = Double(38 * 108)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model108(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model109: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_109", score: Double = Double(38 * 109)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model109(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model110: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_110", score: Double = Double(38 * 110)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model110(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model111: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_111", score: Double = Double(38 * 111)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model111(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model112: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_112", score: Double = Double(38 * 112)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model112(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model113: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_113", score: Double = Double(38 * 113)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model113(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model114: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_114", score: Double = Double(38 * 114)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model114(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model115: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_115", score: Double = Double(38 * 115)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model115(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model116: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_116", score: Double = Double(38 * 116)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model116(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model117: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_117", score: Double = Double(38 * 117)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model117(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model118: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_118", score: Double = Double(38 * 118)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model118(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model119: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_119", score: Double = Double(38 * 119)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model119(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model120: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_120", score: Double = Double(38 * 120)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model120(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model121: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_121", score: Double = Double(38 * 121)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model121(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model122: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_122", score: Double = Double(38 * 122)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model122(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model123: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_123", score: Double = Double(38 * 123)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model123(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model124: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_124", score: Double = Double(38 * 124)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model124(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model125: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_125", score: Double = Double(38 * 125)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model125(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model126: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_126", score: Double = Double(38 * 126)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model126(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model127: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_127", score: Double = Double(38 * 127)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model127(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model128: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_128", score: Double = Double(38 * 128)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model128(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model129: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_129", score: Double = Double(38 * 129)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model129(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model130: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_130", score: Double = Double(38 * 130)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model130(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model131: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_131", score: Double = Double(38 * 131)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model131(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model132: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_132", score: Double = Double(38 * 132)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model132(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model133: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_133", score: Double = Double(38 * 133)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model133(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model134: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_134", score: Double = Double(38 * 134)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model134(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model135: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_135", score: Double = Double(38 * 135)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model135(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model136: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_136", score: Double = Double(38 * 136)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model136(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model137: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_137", score: Double = Double(38 * 137)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model137(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model138: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_138", score: Double = Double(38 * 138)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model138(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model139: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_139", score: Double = Double(38 * 139)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model139(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model140: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_140", score: Double = Double(38 * 140)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model140(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model141: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_141", score: Double = Double(38 * 141)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model141(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model142: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_142", score: Double = Double(38 * 142)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model142(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model143: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_143", score: Double = Double(38 * 143)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model143(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model144: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_144", score: Double = Double(38 * 144)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model144(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model145: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_145", score: Double = Double(38 * 145)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model145(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model146: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_146", score: Double = Double(38 * 146)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model146(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model147: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_147", score: Double = Double(38 * 147)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model147(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model148: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_148", score: Double = Double(38 * 148)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model148(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model149: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_149", score: Double = Double(38 * 149)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model149(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model150: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_150", score: Double = Double(38 * 150)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model150(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model151: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_151", score: Double = Double(38 * 151)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model151(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model152: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_152", score: Double = Double(38 * 152)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model152(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model153: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_153", score: Double = Double(38 * 153)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model153(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model154: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_154", score: Double = Double(38 * 154)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model154(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model155: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_155", score: Double = Double(38 * 155)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model155(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model156: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_156", score: Double = Double(38 * 156)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model156(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model157: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_157", score: Double = Double(38 * 157)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model157(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model158: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_158", score: Double = Double(38 * 158)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model158(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model159: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_159", score: Double = Double(38 * 159)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model159(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model160: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_160", score: Double = Double(38 * 160)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model160(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model161: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_161", score: Double = Double(38 * 161)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model161(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model162: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_162", score: Double = Double(38 * 162)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model162(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model163: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_163", score: Double = Double(38 * 163)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model163(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model164: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_164", score: Double = Double(38 * 164)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model164(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model165: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_165", score: Double = Double(38 * 165)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model165(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model166: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_166", score: Double = Double(38 * 166)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model166(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model167: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_167", score: Double = Double(38 * 167)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model167(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model168: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_168", score: Double = Double(38 * 168)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model168(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model169: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_169", score: Double = Double(38 * 169)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model169(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model170: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_170", score: Double = Double(38 * 170)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model170(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model171: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_171", score: Double = Double(38 * 171)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model171(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model172: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_172", score: Double = Double(38 * 172)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model172(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model173: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_173", score: Double = Double(38 * 173)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model173(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model174: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_174", score: Double = Double(38 * 174)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model174(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model175: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_175", score: Double = Double(38 * 175)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model175(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model176: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_176", score: Double = Double(38 * 176)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model176(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model177: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_177", score: Double = Double(38 * 177)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model177(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model178: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_178", score: Double = Double(38 * 178)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model178(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model179: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_179", score: Double = Double(38 * 179)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model179(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model180: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_180", score: Double = Double(38 * 180)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model180(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model181: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_181", score: Double = Double(38 * 181)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model181(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model182: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_182", score: Double = Double(38 * 182)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model182(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model183: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_183", score: Double = Double(38 * 183)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model183(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model184: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_184", score: Double = Double(38 * 184)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model184(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model185: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_185", score: Double = Double(38 * 185)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model185(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model186: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_186", score: Double = Double(38 * 186)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model186(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model187: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_187", score: Double = Double(38 * 187)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model187(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model188: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_188", score: Double = Double(38 * 188)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model188(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model189: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_189", score: Double = Double(38 * 189)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model189(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model190: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_190", score: Double = Double(38 * 190)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model190(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model191: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_191", score: Double = Double(38 * 191)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model191(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model192: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_192", score: Double = Double(38 * 192)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model192(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model193: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_193", score: Double = Double(38 * 193)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model193(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model194: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_194", score: Double = Double(38 * 194)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model194(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model195: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_195", score: Double = Double(38 * 195)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model195(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model196: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_196", score: Double = Double(38 * 196)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model196(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model197: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_197", score: Double = Double(38 * 197)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model197(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model198: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_198", score: Double = Double(38 * 198)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model198(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model199: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_199", score: Double = Double(38 * 199)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model199(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model200: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_200", score: Double = Double(38 * 200)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model200(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model201: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_201", score: Double = Double(38 * 201)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model201(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model202: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_202", score: Double = Double(38 * 202)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model202(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model203: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_203", score: Double = Double(38 * 203)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model203(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model204: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_204", score: Double = Double(38 * 204)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model204(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model205: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_205", score: Double = Double(38 * 205)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model205(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model206: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_206", score: Double = Double(38 * 206)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model206(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model207: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_207", score: Double = Double(38 * 207)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model207(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model208: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_208", score: Double = Double(38 * 208)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model208(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model209: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_209", score: Double = Double(38 * 209)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model209(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model210: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_210", score: Double = Double(38 * 210)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model210(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model211: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_211", score: Double = Double(38 * 211)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model211(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model212: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_212", score: Double = Double(38 * 212)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model212(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model213: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_213", score: Double = Double(38 * 213)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model213(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model214: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_214", score: Double = Double(38 * 214)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model214(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model215: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_215", score: Double = Double(38 * 215)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model215(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model216: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_216", score: Double = Double(38 * 216)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model216(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model217: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_217", score: Double = Double(38 * 217)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model217(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model218: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_218", score: Double = Double(38 * 218)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model218(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model219: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_219", score: Double = Double(38 * 219)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model219(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model220: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_220", score: Double = Double(38 * 220)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model220(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model221: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_221", score: Double = Double(38 * 221)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model221(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model222: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_222", score: Double = Double(38 * 222)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model222(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model223: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_223", score: Double = Double(38 * 223)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model223(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model224: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_224", score: Double = Double(38 * 224)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model224(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model225: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_225", score: Double = Double(38 * 225)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model225(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model226: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_226", score: Double = Double(38 * 226)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model226(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model227: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_227", score: Double = Double(38 * 227)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model227(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model228: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_228", score: Double = Double(38 * 228)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model228(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model229: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_229", score: Double = Double(38 * 229)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model229(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model230: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_230", score: Double = Double(38 * 230)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model230(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model231: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_231", score: Double = Double(38 * 231)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model231(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model232: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_232", score: Double = Double(38 * 232)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model232(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model233: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_233", score: Double = Double(38 * 233)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model233(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model234: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_234", score: Double = Double(38 * 234)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model234(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model235: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_235", score: Double = Double(38 * 235)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model235(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model236: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_236", score: Double = Double(38 * 236)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model236(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model237: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_237", score: Double = Double(38 * 237)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model237(id: \(id), name: \(name), score: \(score))"
        }
    }

    public struct Model238: Equatable {
        public let id: Int
        public let name: String
        public let score: Double

        public init(id: Int = 38, name: String = "item_38_238", score: Double = Double(38 * 238)) {
            self.id = id
            self.name = name
            self.score = score
        }

        public func normalizedScore(against maxScore: Double) -> Double {
            guard maxScore > 0 else { return 0 }
            return score / maxScore
        }

        public func describe() -> String {
            "Model238(id: \(id), name: \(name), score: \(score))"
        }
    }

    public static func value() -> Int {
        _ = Model1().describe()
        _ = Model2().describe()
        _ = Model3().describe()
        _ = Model4().describe()
        _ = Model5().describe()
        _ = Model6().describe()
        _ = Model7().describe()
        _ = Model8().describe()
        _ = Model9().describe()
        _ = Model10().describe()
        _ = Model11().describe()
        _ = Model12().describe()
        _ = Model13().describe()
        _ = Model14().describe()
        _ = Model15().describe()
        _ = Model16().describe()
        _ = Model17().describe()
        _ = Model18().describe()
        _ = Model19().describe()
        _ = Model20().describe()
        _ = Model21().describe()
        _ = Model22().describe()
        _ = Model23().describe()
        _ = Model24().describe()
        _ = Model25().describe()
        _ = Model26().describe()
        _ = Model27().describe()
        _ = Model28().describe()
        _ = Model29().describe()
        _ = Model30().describe()
        _ = Model31().describe()
        _ = Model32().describe()
        _ = Model33().describe()
        _ = Model34().describe()
        _ = Model35().describe()
        _ = Model36().describe()
        _ = Model37().describe()
        _ = Model38().describe()
        _ = Model39().describe()
        _ = Model40().describe()
        _ = Model41().describe()
        _ = Model42().describe()
        _ = Model43().describe()
        _ = Model44().describe()
        _ = Model45().describe()
        _ = Model46().describe()
        _ = Model47().describe()
        _ = Model48().describe()
        _ = Model49().describe()
        _ = Model50().describe()
        _ = Model51().describe()
        _ = Model52().describe()
        _ = Model53().describe()
        _ = Model54().describe()
        _ = Model55().describe()
        _ = Model56().describe()
        _ = Model57().describe()
        _ = Model58().describe()
        _ = Model59().describe()
        _ = Model60().describe()
        _ = Model61().describe()
        _ = Model62().describe()
        _ = Model63().describe()
        _ = Model64().describe()
        _ = Model65().describe()
        _ = Model66().describe()
        _ = Model67().describe()
        _ = Model68().describe()
        _ = Model69().describe()
        _ = Model70().describe()
        _ = Model71().describe()
        _ = Model72().describe()
        _ = Model73().describe()
        _ = Model74().describe()
        _ = Model75().describe()
        _ = Model76().describe()
        _ = Model77().describe()
        _ = Model78().describe()
        _ = Model79().describe()
        _ = Model80().describe()
        _ = Model81().describe()
        _ = Model82().describe()
        _ = Model83().describe()
        _ = Model84().describe()
        _ = Model85().describe()
        _ = Model86().describe()
        _ = Model87().describe()
        _ = Model88().describe()
        _ = Model89().describe()
        _ = Model90().describe()
        _ = Model91().describe()
        _ = Model92().describe()
        _ = Model93().describe()
        _ = Model94().describe()
        _ = Model95().describe()
        _ = Model96().describe()
        _ = Model97().describe()
        _ = Model98().describe()
        _ = Model99().describe()
        _ = Model100().describe()
        _ = Model101().describe()
        _ = Model102().describe()
        _ = Model103().describe()
        _ = Model104().describe()
        _ = Model105().describe()
        _ = Model106().describe()
        _ = Model107().describe()
        _ = Model108().describe()
        _ = Model109().describe()
        _ = Model110().describe()
        _ = Model111().describe()
        _ = Model112().describe()
        _ = Model113().describe()
        _ = Model114().describe()
        _ = Model115().describe()
        _ = Model116().describe()
        _ = Model117().describe()
        _ = Model118().describe()
        _ = Model119().describe()
        _ = Model120().describe()
        _ = Model121().describe()
        _ = Model122().describe()
        _ = Model123().describe()
        _ = Model124().describe()
        _ = Model125().describe()
        _ = Model126().describe()
        _ = Model127().describe()
        _ = Model128().describe()
        _ = Model129().describe()
        _ = Model130().describe()
        _ = Model131().describe()
        _ = Model132().describe()
        _ = Model133().describe()
        _ = Model134().describe()
        _ = Model135().describe()
        _ = Model136().describe()
        _ = Model137().describe()
        _ = Model138().describe()
        _ = Model139().describe()
        _ = Model140().describe()
        _ = Model141().describe()
        _ = Model142().describe()
        _ = Model143().describe()
        _ = Model144().describe()
        _ = Model145().describe()
        _ = Model146().describe()
        _ = Model147().describe()
        _ = Model148().describe()
        _ = Model149().describe()
        _ = Model150().describe()
        _ = Model151().describe()
        _ = Model152().describe()
        _ = Model153().describe()
        _ = Model154().describe()
        _ = Model155().describe()
        _ = Model156().describe()
        _ = Model157().describe()
        _ = Model158().describe()
        _ = Model159().describe()
        _ = Model160().describe()
        _ = Model161().describe()
        _ = Model162().describe()
        _ = Model163().describe()
        _ = Model164().describe()
        _ = Model165().describe()
        _ = Model166().describe()
        _ = Model167().describe()
        _ = Model168().describe()
        _ = Model169().describe()
        _ = Model170().describe()
        _ = Model171().describe()
        _ = Model172().describe()
        _ = Model173().describe()
        _ = Model174().describe()
        _ = Model175().describe()
        _ = Model176().describe()
        _ = Model177().describe()
        _ = Model178().describe()
        _ = Model179().describe()
        _ = Model180().describe()
        _ = Model181().describe()
        _ = Model182().describe()
        _ = Model183().describe()
        _ = Model184().describe()
        _ = Model185().describe()
        _ = Model186().describe()
        _ = Model187().describe()
        _ = Model188().describe()
        _ = Model189().describe()
        _ = Model190().describe()
        _ = Model191().describe()
        _ = Model192().describe()
        _ = Model193().describe()
        _ = Model194().describe()
        _ = Model195().describe()
        _ = Model196().describe()
        _ = Model197().describe()
        _ = Model198().describe()
        _ = Model199().describe()
        _ = Model200().describe()
        _ = Model201().describe()
        _ = Model202().describe()
        _ = Model203().describe()
        _ = Model204().describe()
        _ = Model205().describe()
        _ = Model206().describe()
        _ = Model207().describe()
        _ = Model208().describe()
        _ = Model209().describe()
        _ = Model210().describe()
        _ = Model211().describe()
        _ = Model212().describe()
        _ = Model213().describe()
        _ = Model214().describe()
        _ = Model215().describe()
        _ = Model216().describe()
        _ = Model217().describe()
        _ = Model218().describe()
        _ = Model219().describe()
        _ = Model220().describe()
        _ = Model221().describe()
        _ = Model222().describe()
        _ = Model223().describe()
        _ = Model224().describe()
        _ = Model225().describe()
        _ = Model226().describe()
        _ = Model227().describe()
        _ = Model228().describe()
        _ = Model229().describe()
        _ = Model230().describe()
        _ = Model231().describe()
        _ = Model232().describe()
        _ = Model233().describe()
        _ = Model234().describe()
        _ = Model235().describe()
        _ = Model236().describe()
        _ = Model237().describe()
        _ = Model238().describe()
        return 38
    }
}
